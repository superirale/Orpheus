name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            install_deps: |
              sudo apt-get update
              sudo apt-get install -y libasound2-dev libpulse-dev
          - os: macos-latest
            install_deps: echo "No extra deps needed on macOS"
          - os: windows-latest
            install_deps: echo "No extra deps needed on Windows"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: ${{ matrix.install_deps }}

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DORPHEUS_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build --parallel --config Release

      - name: Run tests
        shell: bash
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            if [ -f "./build/Release/orpheus_tests.exe" ]; then
              ./build/Release/orpheus_tests.exe --reporter console
            else
              ./build/Debug/orpheus_tests.exe --reporter console
            fi
          else
            ./build/orpheus_tests --reporter console
          fi

      - name: Verify example builds
        shell: bash
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            if [ -f "./build/Release/orpheus_example.exe" ] || [ -f "./build/Debug/orpheus_example.exe" ]; then
              echo "Example binary built successfully"
            else
              echo "Error: Example binary not found"
              exit 1
            fi
          else
            if [ -f "./build/orpheus_example" ]; then
              echo "Example binary built successfully"
            else
              echo "Error: Example binary not found"
              exit 1
            fi
          fi

  build-android:
    name: Build (Android ${{ matrix.abi }})
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        abi: [arm64-v8a, armeabi-v7a, x86_64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Android NDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'ndk;27.0.11718014'

      - name: Configure CMake for Android
        run: |
          cmake -B build \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_ROOT/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=${{ matrix.abi }} \
            -DANDROID_PLATFORM=android-24 \
            -DCMAKE_BUILD_TYPE=Release \
            -DORPHEUS_BUILD_TESTS=OFF \
            -DORPHEUS_BUILD_EXAMPLES=OFF

      - name: Build
        run: cmake --build build --parallel --config Release

      - name: Verify library built
        run: |
          if [ -f "build/liborpheus.a" ]; then
            echo "Android library built successfully for ${{ matrix.abi }}"
            ls -la build/*.a
          else
            echo "Error: Android library not found"
            exit 1
          fi

  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install analysis tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-tidy cppcheck libasound2-dev libpulse-dev

      - name: Configure CMake (with compile_commands.json)
        run: cmake -B build -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Run clang-tidy
        run: |
          echo "Running clang-tidy on source files..."
          find src include -name '*.cpp' -o -name '*.h' | head -20 | while read file; do
            echo "Checking $file"
            clang-tidy "$file" -p build --quiet 2>/dev/null || true
          done
          echo "clang-tidy analysis complete"

      - name: Run cppcheck
        run: |
          echo "Running cppcheck..."
          cppcheck --enable=warning,style,performance \
                   --suppress=missingIncludeSystem \
                   --suppress=unusedFunction \
                   --error-exitcode=0 \
                   --inline-suppr \
                   -I include \
                   src/ include/
          echo "cppcheck analysis complete"
